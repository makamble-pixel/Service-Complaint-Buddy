/*const express = require('express');
const router = express.Router();
const authMiddleware = require('../middlewares/authMiddleware');
const { createComplaint, getComplaintsByEmail } = require('../controllers/complaintController');

// Define routes
router.post('/submit-complaint', authMiddleware, createComplaint);
router.get('/complaints', getComplaintsByEmail);

module.exports = router;*/

